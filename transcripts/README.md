# Session Transcripts

Auto-generated per Claude Code session. Local only -- not committed to git.

Each file covers one session: BACKLOG.md state + full Pam/JJ conversation.
Delete when no longer needed. Git never sees these files (transcripts/ is gitignored).
